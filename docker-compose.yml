services:
  bot:
    build:
      context: .
      dockerfile: Dockerfile.dev
    image: themoviebot
    container_name: themoviebot
    volumes:
      - .:/bot
    command: "/bin/bash"
    stdin_open: true
    tty: true