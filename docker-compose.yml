services:
  bot:
    build:
      context: .
      dockerfile: Dockerfile.dev
    image: themoviebot
    container_name: themoviebot
    ports:
      - 8000:8000
    volumes:
      - .:/bot
    stdin_open: true
    tty: true